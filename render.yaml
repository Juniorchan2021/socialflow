services:
  - type: web
    name: socialflow
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ANTHROPIC_API_KEY
        sync: false
    plan: free
    branch: main
    healthCheckPath: /api/health
